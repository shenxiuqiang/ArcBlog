kind: AgentGrant
fields:
  agentDid: string
  capabilities: json # spec §61: agent.read | agent.write | agent.publish | agent.economy
  ttlMinutes: number
  grantedAt: datetime
  expiresAt: datetime # authorization is time-boxed, never open-ended
  note: string
  updatedAt: datetime
