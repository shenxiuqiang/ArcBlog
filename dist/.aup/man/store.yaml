# Auto-scaffolded by `arc dsl generate --write` (#1284 Phase 2) — this is a starting
# point, not finished documentation. Fill in description/synopsis/examples/flows,
# then this file is the authoritative man page for "store".
schemaVersion: 1
page: store
description: >-
  TODO: describe what "store" is for and when an agent should use it
  (auto-scaffolded stub — fill in before relying on this man page).
refs:
  actions:
    - id: site-header
      event: user-menu-item
    - id: site-header
      event: nav-click
